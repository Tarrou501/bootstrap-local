# Boostrap-local

 attrib -s -h -r /s /d
 attrib +s +h +r /s /d
   s: sistema
   h:hidden
   r:readonly

git init
git add .
git branch -M develop
git remote add origin https://github.com/Tarrou501/bootstrap-local.git
git -m commit 'doc: added readme'
git push -u origin develop 